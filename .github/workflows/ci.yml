name: Tests for neovim

on: [push, pull_request]

jobs:
  unit_tests:
    name: unit tests
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os:
          - macos-latest
          - windows-latest
          - ubuntu-latest
        version:
          - nightly
          - v0.5.0
    steps:
      - uses: actions/checkout@v2
      - uses: actions/checkout@v2
        with:
          repository: nvim-lua/plenary.nvim
          path: ../plenary.nvim
      - uses: rhysd/action-setup-vim@v1
        id: vim
        with:
          neovim: true
          version: ${{ matrix.version }}
      - name: Run tests
        run: |
          nvim --version
          make test
